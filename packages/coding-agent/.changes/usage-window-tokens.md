- Added the optional models.json `usageWindowTokens` field (model definitions and `modelOverrides`): a rate-quota heuristic clamping the compaction trigger, the summarization budget, the branch-summary budget and `keepRecentTokens` to `min(contextWindow, measured input limit, usageWindowTokens)`. Validated on both the definition and the override path (> 0, <= the effective contextWindow; the only-cap case compares against the 128000 default instead of being silently clamped away).
- The interactive footer shows a `窗限<n>` hint when the configured cap sits below the declared window, and threshold compaction logs one session warning when `reserveTokens` consumes the whole capped base (a typo'd cap lands exactly in that silent zone).
- Documented the field in docs/models.md and docs/compaction.md, including the trade-off (200,000 on a 1M window moves the trigger from 700k to 140k with the default 0.7 ratio) and the rate-quota evidence it is based on.
