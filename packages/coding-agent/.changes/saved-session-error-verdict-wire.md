- The saved-session wire carries an `error` task state now, so an off-daemon (archived) row whose last turn failed reads as failed instead of showing the terminal error recap with no verdict on it. This is the wire half of upstream #2310: the client validator has accepted the value since 8f52777f2 and only `serializeSavedSessionInfo`'s downshift held it back. A client older than the revision that opens it still rejects the whole session item over the new value.
