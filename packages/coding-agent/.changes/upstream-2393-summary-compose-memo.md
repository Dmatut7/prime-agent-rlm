- Fixed daemon CPU growing with session size: the active-session summary compose is now memoized per session (a cheap fingerprint over every summary input, including this fork's kernel-residency fold, stall state and spawn code, decides when to recompose), the latest-activity timestamp scan folds only messages appended since the last scan, and the heartbeats list reads two display labels instead of composing a full summary per registered job per poll. Any live-state change still recomposes and publishes the same wire delta as before.
