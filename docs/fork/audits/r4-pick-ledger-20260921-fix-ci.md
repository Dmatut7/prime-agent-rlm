# R4 拾取台账 · CI 修复席（fix-ci）

> 席位：r4-fix-ci。分支 r4/fix-ci（基线 9cf71140f，worktree /Users/a1/Desktop/ai/pa-r4-fix）。本台账只记本席 CI 红修复件；格式照 runbook §六。SKIP/弃 hunk/撞名处置均记。

| # | 红名/件 | 肇事 sha | 落点 commit | 根因 | 修法 | 定向测试结果 | 备注 |
|---|---|---|---|---|---|---|---|
| 1 | 4167-thinking-toggle-pending-tool-render ×2（keeps unresolved rendered tool calls registered / does not keep completed historical tool calls registered as pending） | 41a4bdf24（U4 轮聚合） | c8ae8a325 | U4 把 settled 工具体藏进轮聚合行（isHiddenByTurnSummary：折叠∧未展开∧step settled→整块隐藏），4167 仍在断言默认折叠视图里可见结果文本；pendingTools 注册/去注册断言全过＝装配未丢，纯渲染可见性 | 测试适配（保命题）：断言前 expandRenderedToolComponents（对齐 U4 自己在 id3-duplicate-tool-call-id 的同款适配）；import type→值导入以 instanceof | 2/2 绿（消毒跑法）；pre-commit 全量 check 过 | 无产品码改动；#14（5d84bccb7）只改 2 行默认明细级别，排除；U4 父提交 d2a920f4c 实证测试原本全绿；test-only 无 fragment（仓惯例） |
| 2 | ma-p0-3-esc-wake ×1（keeps the abandonment driver alive for a routine notice left behind by the wake） | 57bdfed04（#33 re-park prefix） | bbf6250f0 | 不是同一通知双计数：#33 让被 Esc 取消的 wake turn 把 folded failure 通知（prefix 记录）交回 deferred 队列（wake turn 未 dispatch、正文未入 transcript，DIAG 实证 pending=[failure, terminal_notice]、abandoned:1/persisted:1）；abandonment 时 routine 被 abandoned＋failure 被 persist（C3 教义）→count=2。#33 上游明言丢 terminal notice 是 bug，新行为更优（死亡报告存活） | 测试适配（保命题＋强化）：count 2＋断言 failure 通知恰 1 条进 transcript、routine 通知 0 条存活；中段 deferral stamp 不变量原样保留 | 6/6 绿（消毒跑法）；pre-commit 全量 check 过 | U5(315c8240d) 只动 daemon-mode.ts，本测试不经 daemon，排除；test-only 无 fragment |
| 3 | 4491-provider-stale-after-401 ×1（marks captured auth failures stale when the final retryable error is not auth） | 60f7ff393（#35 配额停泊） | fa4c58c7 | 不是 stale 链路断：#35 新增「快速重试耗尽∧transient→unavailable 等待循环」——500 是 transient 类，耗尽后不再终局而是 ping provider（第 4 次调用吃掉脚本响应、最终错误变成 faux 无响应错误）；auth_stale 在等待循环终止后照发（DIAG 实证） | 测试隔离（保命题）：该例 harness 加 provider.waitForUsage.enabled=false（照 retry-events 既有同款隔离写法），断言全数不动（3 次调用、attempts[1,2]、stale、500+login guidance）；等待循环自有覆盖在 agent-session-retry-events.test.ts | 9/9 绿（消毒跑法）；pre-commit 全量 check 过 | waitClass 收紧面（401 auth 仍 permanent 短路禁重发，第一例 401×3 仍 2 次调用绿）实证未坏；test-only 无 fragment |

| 4 | 另任务复验：compaction-emergency-shrink-linear（全量并发红/隔离绿） | —（非回归） | —（不修，回执母席） | 判据＝墙钟比值（growthOverLinear≤2/十年）；纯 CPU 饱和（10×yes）两跑一红一绿：红例 tiers ms=0.4/4.1/89.9 growth=0.91/2.21——小档 best-of 撞净切片、100k 档窗口大躲不开抢占，比值不对称放大贴线闪烁；算法本身仍线性（第一档 0.91，100k 档 89.9ms 仍够快）；本机全量并发两轮（基线+我分支）均绿 | 结论＝假红性质（算法未回归）＋真时延敏感（墙钟判据对负载过敏、贴线闪烁）；建议母席裁：判据改 process.cpuUsage()（CPU 时间免疫抢占，最稳）或独立/串行 lane（照 kernel-heavy 先例）或每十年上限放宽到 3 | 隔离 5/5 绿；饱和 1红1绿；全量×2 绿 | 不改码（修不修等母席回执） |
| 5 | 另任务复验：4603-worker-recovery（全量并发红/隔离绿） | —（运行模式问题） | —（不修，回执母席） | 纯 CPU 饱和（10×yes+vitest）6/6 绿（83.8s，慢例 shutdown --force 30.7s/预算 150s，余量 5×）——时延轴排除；全量并发红机理＝machine-wide 发现互杀（ci.yml 注释明载：daemon ps 经 ss/lsof 找全机 daemon、曾收割 4606 前任致双红，事故 34753200385），CI 早已为此设独立 machine-wide job（test:machine-wide + --no-file-parallelism） | 结论＝假红（非时延超预算）；本地全量（npm test 不带排除）必撞互杀轴，复验永远走 test:machine-wide；无需改码 | 隔离 6/6 绿（67.6s）；饱和 6/6 绿（83.8s） | 我未做全量+4603 同跑（会威胁本机活 daemon，纪律禁 prime-agent shutdown）；结论靠饱和实验+CI 设计文档+事故记录三证 |
| 6 | 全量对账（零新增红声明） | — | —（证据行） | 基线 9cf71140f 全量（test:ci 剔除集）：8 文件 9 红＝任务 4 红（4167×2/ma-p0-3/4491）＋git-context＋settings-ancestor＋2108＋502×2＋6006；我分支全量：5 文件 5 红＝基线红集−任务红，逐名对上 | 我三笔只改三个测试文件、全绿；剩余 5 红分类：git-context＝本机 gitconfig url.insteadOf 伪影（GIT_CONFIG_GLOBAL=/dev/null 即 6/6 绿，CI 无此配置）；6006＝worktree 缺 dist/ 构建产物（bundle.mjs 找不到 dist/cli.js，CI 先 build）；2108+502×2＝母席干线已有修复（dcbea60a6 tui×2 探针重写、67331915a agents-view 列折叠），我基线早于该两笔；settings-ancestor＝全量并发下两分支同红、隔离绿的 fs.watch 时序 flaky（与 compaction 同类） | 全量 161s/8392 tests；日志 /tmp/r4_fix-ci_full-suite_20260922_112833.log（我）与 /tmp/r4_fix-ci_base-full_20260922_113845.log（基线） | 无 |
| 7 | 母席终判落地：compaction 判据改 process.cpuUsage()（方案一） | —（修 flaky 判据，非肇事笔） | 7f33f4aa8 | 母席终判：判据语义不变（每十年线性预算≤2），测量从墙钟换 CPU 时间（user+system，换算 ms、detail 标签 cpuMs）；墙钟判据在 10 核饱和下小档撞净切片、100k 档躲不开抢占致比值贴线闪烁（0.91/2.21 一红一绿），CPU 时间免疫抢占 | timeBestOf 换 process.cpuUsage()；预算与全部语义断言不动；正控保留（旧二次方走查仍被 judge 判红＝判据没变瞎） | 隔离 5/5 绿（含正控）；饱和×2 2/2 绿（对照墙钟 1红1绿）；pre-commit 全量 check 过 | 独立 lane 方案母席不取（成本大）；test-only 无 fragment |
| 8 | 母席终判留档：4603 不动、settings-ancestor 留档 | — | —（不修） | 母席终判：4603 CI 已有 machine-wide 专用 lane，不动；settings-ancestor 按本地并发伪影记档不修（两分支全量同红、隔离绿，fs.watch 时序类） | — | — | 本行即留档 |
