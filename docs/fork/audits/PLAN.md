# 当前计划（2026-09-15 05:00 起，母席自控）

## 目标（长期，不叫停不停）
持续审 → 多模型复核 → 只把站得住的写进带日期文档 → 能修的修（先红后绿）→ 修完推 CI 并**用 CI 工件验证**。
质量闸：①先红后绿 ②≥2 个不同模型 + 母席复算 ③全仓 check + tsgo ④推送后看 CI 具体 run，不看"最新一次"。
速度闸：同时 ≤4 条线；每条 ≤60s 探针、先写报告再回话；收线后扫孤儿（`ppid=1` 的 tsx）。

## 排队中（按优先级）
1. **CI 红修复收尾**：本条已在 `070a3e0b9` 处理（把新内核测试移出分片、留在 kernel job）；观察下一次 CI 的 shard 2 闸是否回绿。
2. **切界面慢 + 瞬时高 CPU（老板亲报）**：性能线已量化——切界面 = `listAll`(102 会话/503MB) + `listPassiveRlmSubagents`(853 活边→628 转录/1042MB 逐个 readSessionInfo) 两条读盘路径；冷态 1.75s + 4.02s；UI 每帧全量 reconcile 在 N=500 时单次拷 32MB 字符串 = 瞬 CPU 主因。
   修法（预估）：passive summary + 活边集合加 5~10s TTL（切界面 207ms→<20ms、冷 4.02s→~0.1s）、UI reconcile 50ms 节流 + 增量行、attach 首帧按需（1.6s→0.3s，去掉 26 次强制落盘）、ledger 死边 prune（853 里 225 条指向已删文件）。
3. **磁盘**：已回收 ≈6.6GB（dill 2.95GB + 工作树 2.28GB + 归档工作树 1.4GB + 退役 venv 0.23GB）；保留策略施工在跑（目标：artifacts 只回收未引用代际、TMPDIR 分族、删了又回来根治）。
4. **待修清单**（已确认、未修）：工具名静默顶替/重复 tool id（补丁在工作树，收割中）、停机入场券 fail-open、shutdown 作用域闸、子代理配额、供应链 B/E/D、S1 残留、X1（win32 内核路径 P1）、ID-2/TP-2/UI-3 等。
5. **审查线**：今晚代码的异构审查已在跑（2×K3 + 1×千问），回话后按闸筛选并档。

## 我自己发明的节奏
- 每轮心跳：收线 → 复算 → 并档 → 派线（≤4）→ 扫孤儿 → 报一句进度。
- 每次推送后：查**该 run** 的 job 明细；红则先修红（像今天这样），再回到主线。
- 磁盘告急时优先做回收，不新起大扫描（审计期禁止重复冷扫同一批转录——已实测并发冷扫同一文件 137ms→2337ms，17× 漂移，正是 CPU 尖峰来源）。
