# R4 拾取台账 · 岛席 B（r4/island-b）

格式：`| # | sha | 落点commit | 冲突文件数 | 真MD处置 | 撞名/弃hunk | 定向测试结果 | 备注 |`

| # | sha | 落点commit | 冲突文件数 | 真MD处置 | 撞名/弃hunk | 定向测试结果 | 备注 |
|---|---|---|---|---|---|---|---|
| 2 | 756a8ce83 | 6d2d6dca9 | 3（均为 modify/delete） | 3 件 MD 按上游后像重建：README.md／worker.py／tests/test_benchmarks.py（67334bf1a P3 弃置面）＋恢复 tests/calibration.json 作重建套件 fixture | controller.py FILES hunk 适配去 ui.py（fork 无此件，P3 既有缺口）；上游 fragment cb-perf-switch-resume.md 去重不落（0.10.1 已记账）→ 换适配 fragment upstream-2399-frame-decode-bench.md | session-worker-private-framing.test.ts 7/7 绿；benchmark 套件 ruff check/format 12 件净＋unittest discover 42/42 绿；frame-decode-bench.mjs 对真 dist 冒烟 32MiB/8KiB=8ms RESULT 正常 | 核心 src+test 已由 1bef2a01a 带入（本笔零 diff 合并），本笔只落 benchmark 半件；ui harness 缺口为既有 P3 弃置，非本笔引入 |
| 4 | 7572880fe | 473d708f4 | 2（daemon-ps.ts 重写面＋test import 面） | 无真MD（3件全在场；fragment 为 A 直落） | src：pattern 常量与早退取上游（正则逐字节同）＋fork 目录证据体保留＋按上游意图去 win32 守卫（fork 实际管名已交叉核对：key=sha256 前12hex、workerId=尾12hex）；test：import 冲突取 fork 解析器导入、新 pipe it() 逐字落、planReap 简化 hunk 自动并（保留 fork 第三参 MACHINE_STOP_SELECTION） | daemon-ps.test.ts 28/28 绿（27→28：+1 pipe 向量、-1 上游合并的过矩阵向量） | 预提交钩子全仓检查过 |
| 25 | d2cdb4504 | —（已应用：74ae2ff64，09-20 fork 自移植） | 2（src 内容冲突 11 块＋test AA 撞名） | 无真MD（3 件全在场） | 撞名件 cron-jobs-state-snapshot.test.ts 处置＝保 fork 版：用例集与上游同名同序（fork 版含 parse-path 冻结强化与 CronJobsWriteDroppedError raced-write 适配，为上游全集＋fork 扩展），上游字面行与 fork store 形状冲突故禁盲并；src 11 冲突块全取 ours（fork 移植已含上游二审三修，16 个 snapshot 调用点对上游 14 读点）；上游 fragment cron-catalog-mtime-snapshot.md 去重不落（0.10.1 已发版记账） | cron-jobs-state-snapshot.test.ts 11/11 绿（fork 版现状） | 三面全解为 fork 后 staged diff 为空→cherry-pick --skip（机械证明已全量在场）；「合并两侧内容」以语义全集形式完成 |
| 29 | e683fcb53 | caa098953 | 2（src 重分叉面＋test 重分叉面，均手工移植解） | 无真MD（3 件全在场） | fork 未携 #2314 地基（67334bf1a 裁定 keep-ours，/tmp/p3as-file-rulings.md L178）→ 本笔全量移植：chain-reset 单次重试＋#2374 修复件（delete responseId＋isCodexVisibleResponseEvent 门控）逐字落，重试环尾接 fork SSE 回退语义；test 移植 installScriptedCodexWebSocket 等四 helper＋两个 chain-reset 测试（其一为 fork 新覆盖）＋SSE 开体测试按上游合并；fragment 上游 bullet 扩写为 fork 实际可见变化 | openai-codex-stream.test.ts 19/19 绿（17→19）；tsgo --noEmit 净 | 基线备注：clamp 三测本机空洞绿（mock 内断言失败被重试吞、17.5s×3）＝HEAD 既有，非本笔回归；套件全跑 157s |
